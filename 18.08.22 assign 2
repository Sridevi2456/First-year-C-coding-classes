#include <stdio.h>
int main()
{
    int n,i,j,arr[10],f=1,sum=0;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
    for(i=1;i<=n;i++)
    {
        f=f*i;
       sum=sum+f;
    }
    printf("%d",sum);
    return 0;
}
